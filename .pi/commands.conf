# Suckless Pi - Commands Configuration
# Format:
#   alias <shortcut> <original-command>
#   group <name> <command...>
#   hide <command>
#
# This file contains PROJECT DEFAULTS. Override in ~/.pi/commands.conf
#
# See SUCKLESS_EXTENSIONS_MANAGEMENT.md for details

# ============================================================================
# COMMAND ALIASES (project shortcuts)
# ============================================================================
# alias fb /browse
# alias rw /review

# ============================================================================
# COMMAND GROUPS (for /help organization)
# ============================================================================
group search /brave-search
group files /browse /files
group dev /extension-dev

# ============================================================================
# HIDDEN COMMANDS (internal/debug, not shown in help)
# ============================================================================
# hide /debug-command
